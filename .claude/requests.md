# Request log

A running record of notable requests, the decisions made, and outcomes. Newest at top.

| Date | Request | Decision / outcome |
|------|---------|--------------------|
| 2026-06-13 | Resume faction sweeps | **Resistance** batch (now with the full hook toolkit): 10 pilots — Covenell/Nimi Chireen/Zay Versio/Lulo Lampar (cost-free dice mods, auto), Joph Seastriker (onShieldLost), Rey/Finn (cost: Force/strain, offered), Zizi Tlo (after-attack/defend charge→focus), Jay Cristubbs/Seftin Vanik (no-cost token effects via target-select). 189 tests green; redeployed after the faction. Next faction: Galactic Republic or Scum. |
| 2026-06-13 | Do the next hooks in one sweep | **Phase-start ability windows** (R4-M9): a `nextPhaseOffer` driver in the reduce loop offers `onSystemPhase`/`onEngagementStart` abilities in initiative order, one at a time, tracked done per phase (reset on PhaseAdvanced). **von Reg** (System → strain a bullseye enemy) + **Muse** (engagement start → clear a wingmate's stress) ride it via the target-select primitive. 185 tests green. **9 reusable hooks done.** Remaining (bigger/lower-leverage): **condition cards** (subsystem), setup window, moved-through triggers. |
| 2026-06-13 | Keep building hooks | **Target-select effect primitive** (R4-M9): `offerTargetEffect` + a `select-target` decision + serialisable `EffectSpec` (transfer/grant/remove token) applied to a chosen ship deterministically. **Axe** rides it (pass a green token to a side-arc wingmate). Unlocks the choose-a-ship-and-token class. 182 tests green. **8 reusable hooks done** (reactive windows, bonus attacks, dynamic initiative, attacker-context, deplete, after-destroyed, lockdown, target-select). Remaining: setup window, conditions, a System-/Engagement-Phase ability window (to trigger target-select for von Reg/Muse/Holo), moved-through. |
| 2026-06-13 | Keep building hooks | Two more **R4-M9** hooks: **after-destroyed window** (`onDestroyed` fired for living friendlies of a killed ship → Avenger free action via ActionGranted); **dice lockdown** (`lockdown` ability hook suppresses a ship's whole modify step — spends, offered abilities, and auto cost-free abilities — across combatSpends/combatAbilities/applyAttackAbilities/finishCombat → Midnight). 180 tests green. Remaining: setup window, conditions, token transfer, deplete/strain-granting triggers, moved-through, System-Phase window. |
| 2026-06-13 | Keep building remaining hooks | Three more **R4-M9** hooks: **dynamic initiative** (`initiative` hook + `effectiveInitiative` in the order sorts — Null/Rush); **attacker context** threaded through the optional-ability path so defender reactive windows know who shot them — **Dengar** returns fire (afterDefend → charge → bonus attack vs attacker); **deplete token** (attack-side strain: −1 attack die, spent on attack, shed by blue maneuver). 178 tests green. Remaining hooks: setup window, conditions, token transfer, deplete/strain-granting triggers, dice-lockdown (Midnight), after-destroyed auras, moved-through triggers. |
| 2026-06-13 | Build the missing mechanics before switching faction | New milestone **R4-M9**. **T4-R1 ✅ reactive windows** — `afterAttack`/`afterDefend`/`onDamaged`/`onShieldLost` fired when an attack resolves (`appendWindow` queues ≤1 offer); reactive abilities reuse the action-grant flow (Tavson). **T4-R2 ✅ bonus attacks** — `offerBonusAttack` → a declare-attack that reuses interactive combat but a `bonus` flag keeps it from spending engagement (Quickdraw). 174 tests green. **T4-R3 remaining:** setup window, condition cards, token transfer, deplete tokens, dynamic initiative, dice-lockdown, after-destroyed auras, moved-through triggers; also pass the attacker into the defender's reactive windows (Dengar). |
| 2026-06-13 | Grind sweep by faction; redeploy after each | **First Order** done for current mechanics: registered the cleanly-implementable pilots — Backdraft / Blackout / Longshot (cost-free, auto), DT-798 / Static (cost, offers); Scorch already done. The other ~35 FO abilities need hooks we lack: **setup window**, **after-destroyed / after-damaged / after-shield-loss triggers**, **bonus attacks**, **condition cards**, **token transfer**, **deplete/strain-on-others**, **dynamic initiative** (Null/Rush), **dice-can't-be-modified** (Midnight/Ember). 170 tests green; redeployed after the faction. **Next faction:** Resistance (66 pilots) or Galactic Republic. |
| 2026-06-13 | Push/redeploy T4-C2, resume M7 sweep | Pushed + redeployed (server `5a867dc0`, client 200). Resumed the sweep, now classifying by **cost**: registered **Ric Olié** (cost-free → auto), and three cost offers on the `optionalAttack` path — **"Scorch"** (stress for a die), **Darth Vader/BoY** (Force → blank to hit), **Ezra Bridger** (Force → up to 2 focus changes; defence offer gated to when he's the defender). All original paraphrases. 166 tests green; committed `ae877b3` (not yet redeployed — batching deploys). |
| 2026-06-13 | Refine: optionality is by **cost**, not "may"; auto-skip empty steps | **T4-C2 ✅**. Only resource-costing abilities (spend focus/Force/charge) are optional; cost-free effects (extra/fewer dice, cost-free rerolls/changes) auto-trigger (lightweight). Reverted Predator/Marksmanship/Fanatical to auto. New **`after-defence`** combat step lets the attacker use cost abilities on the defender's dice → migrated **Crack Shot** to an offer. **Auto-skip** via exported `trivialCommand` (no target → pass, no action → skip, nothing to spend → proceed), applied in the client store so the engine FSM stays pure (first tried it in `reduce` — broke FSM-inspecting tests, so moved it to presentation). 162 tests green. Sweep can now register cost cards correctly. |
| 2026-06-13 | Push, redeploy, start T4-C2 | Pushed to origin/main; redeployed server (`63295681`) + client (200 OK). **T4-C2 first slice ✅**: new `optionalAttack` ability section — "may" dice effects are offered during the modify step (each ≤ once/attack via `usedAbilities`), with **rerolls only before any result change** (`changed` flag on combat). `UseModifyAbility` command; modify decision lists abilities. Migrated **Predator** (reroll), **Marksmanship**, **Fanatical**. Still auto (need richer modify-defence timing / roll-step offers): Juke, Crack Shot, Howlrunner, Heroic, "may +die" cards. 161 tests green. |
| 2026-06-13 | Spends/abilities are optional ("may"), not auto | Correct — the engine wrongly auto-applied focus/evade/calculate/Force/lock-reroll + "may" abilities. New milestone **R4-M8 interactive combat**, staged **tokens first**. **T4-C1 ✅**: combat is now event-sourced + interactive — `state.combat` holds an in-progress attack, pausing for a `modify` decision (attacker step, then defender) where token spends are discrete `Modify`/`ModifyDone` choices; the dice pool folds deterministically via `CombatBegan`/`CombatDiceSet`/`CombatAdvanced`/`CombatEnded` (golden-master tests still pass). `resolveAttack` kept atomic for tests/AI. Client modify panel added. 159 tests green. **T4-C2 next:** migrate the 14 registered abilities to optional offers (Crack Shot/Predator/Force-spend), rerolls strictly before changes — *then* resume the M7 sweep registering "may" cards correctly. |
| 2026-06-13 | Register remaining cards (M7 sweep) + verify quick builds | **Quick builds:** present in xwing-data2 (preset combos) but not our snapshot; **69** of their upgrades aren't in the XWA feed (quick-build-only/scenario reprints). Generator now flags upgrades `available`; `upgradeOptions` hides non-available ones. **Sweep scale:** ~970 cards have abilities — too many for one pass, and many need engine hooks we lacked. **User chose: build hooks first.** Registered a first clean batch (Gideon Hask, Graz, Ahhav, Lt. Blount, Laetin A'shera, "Night Beast") as original paraphrases. **Built the cross-ship action-grant hook** (`grantOffer` + `grant-target` decision → coordinate `grantedAction` flow, optional Force cost); Ahsoka Tano + Airen Cracken ride it. 155 tests green. **Next hooks:** setup window; confirm Force-spend attack abilities as auto-spend (no new hook). |
| 2026-06-13 | Sort out linked actions before the M7 sweep | The generator dropped xwing-data2's `linked` field. Fixed `generate.mjs` to capture it + **regenerated** the snapshot (cloned xwing-data2 @ `0dc446f` + xwing-points @ 50P20, both matching the pin; Prettier-formatted so the diff is *only* the added `linked` fields). Threaded **linked actions** through the engine: `actionLinks` on the ship; after a base action the FSM pauses (`LinkOffered`/`LinkResolved` + `linkedAction` state) to offer the one linked follow-up (use/skip), run through the full action path (linked repositions hit the placement choice; linked locks target an enemy) and charged at the link's own difficulty (red→stress). Links don't chain. Full gate green (147 tests); redeployed. **T4-S2 + R4-M4 fully closed.** |
| 2026-06-12 | Finish T4-S2 + deploy | **T4-S2 ✅ / R4-M4 ✅**. Added **SLAM** (reuses the reposition FSM: any dial maneuver at the executed speed → move + disarm + red-stress) and **red/purple action costs** from card data via a new `actionDifficulty` on the ship (red → stress; purple → spend Force + hidden without it). **Linked actions deferred** — the `linked` field isn't in the committed data snapshot (needs a regen). Full gate green (144 tests). Then deployed (see below). |
| 2026-06-12 | R4-M4 (ability scope + full action system) | **T4-S1 ✅** auras: `gatherAttackHooks` gathers from all living ships (existing abilities already guard on `self`, so safe); added `inRange` + **Howlrunner** reroll aura. **T4-S2 ◐** full action bar, landed in 3 commits: **boost/barrel-roll** as real repositions (new `reposition.ts` + `reposition` pending decision, collision-pruned); **jam** (range-1 enemy gains a jam token) + **reload** (charge + disarm); **coordinate** (grant a friendly at range 1–2 a free self-action via a `grantedAction` FSM pause, without consuming its own activation action). 4 commits, full gate green (141 tests). **Remaining in T4-S2: SLAM, red/linked actions** (the latter needs per-action difficulty in the model). Also deferred: coordinate-granted repositions/locks, and reusing the reposition system for decloak's barrel-roll + tractor. |
| 2026-06-12 | R4-M3 (arcs) | **R4-M3 ✅**. `arcs.ts` generalised to `inArcAt(centre, half)` + arc kinds front/rear/full-front/single-turret/double-turret (bullseye existed); `attackValue(a,t)` = best value across the ship's arcs or null. Engine `Ship` gained `arcs`+`turretArc`; `build.ts` derives them from card attack stats (ARC-170 front 3/rear 2, decimator rotatable double-turret, etc.). New **`rotate-arc` action** → `ArcRotated` event; `nextFacing` cycles single turrets / toggles double turrets. Combat now rolls dice from the bearing arc; engagement only targets what `attackValue` reaches. Client: action label + log line; sandbox round-trip carries arcs. 7 new arc tests incl. an end-to-end rotate. Full gate green (130 tests). Deferred: setup facing-picker UI, explicit rotate-to-any (cycles for now), board viz of non-front arcs. **Next: R4-M4** auras + full action bar (with real boost/barrel-roll). |
| 2026-06-12 | R4-M2 T4-N2 (cloak) | **T4-N2 ✅** (RR-grounded). Cloak action → blue cloak token; cloaked = **+2 agility on defence**, **disarmed** (folded into `isDisarmed`), and can't re-cloak. **Decloak** added as a **System-Phase** optional decision: new `decloak` pending + `Decloak`/`SkipDecloak` commands + `Decloaked`/`DecloakPassed` events, sequenced low→high initiative via a new optional `hasSystemActed` ship flag (reset at RoundEnded). Decloak spends the token and does a forward boost (small [2] / med-large [1]) via `pathAt`+`collides`; an overlapping decloak fails (revert, keep token). Cloak is blue so it survives the End Phase. Client: decloak control, cloak action label + chip. Exhaustive switches updated (harness, replay chooser, log). Full gate green (123 tests). **R4-M2 complete.** Deferred to M4: full decloak menu (barrel-roll + direction). **Next: R4-M3** arcs. |
| 2026-06-12 | Continue R4-M2 (negative status tokens) + reconcile rulings vs xwing.life | **T4-N1 ✅**, then **corrected against the XWA RR v1.4.6** (downloaded the 75 MB PDF, extracted text via a pure-Python zlib script since no pdftotext/pip). First pass had several wrong rulings; fixes: **ion** plans normally and executes a **blue speed-1 in the dial's direction** (not white-straight, not skip-planning), is **calculate-only**, sheds ion **at end of activation**, and **breaks its locks** on ionisation; **tractor** is a **threshold** state (small 1/med 2/large 3) giving −1 defence die with **no large exemption**; **strain** is **not** cleared in the End Phase (red); **disarm/tractor/jam** (orange, circular) are **fully** cleared in the End Phase; **jam** is a real persistent token that lingers and eats the next green/lock when none present. Centralised in `tokens.ts`. Full gate green (119 tests). **Next: T4-N2** cloak. |
| 2026-06-12 | Scope + start R4 (full rules surface) | Scoped `docs/r4-backlog.md`. Decisions: sliced **R4a (M1–M4)** then **R4b (M5–M6)**; **full token rules-effects** (not markers); start M1. **R4-M1 ✅**: calculate (per-die focus), reinforce (shave a 2+ hit), **Force** (pilot pool, spent like focus, recovers/round), **per-card charge pools** (`upgradeCharges` keyed by xws so one card can't spend another's). Next: **R4-M2** negative status tokens (ion/tractor/disarm/strain/jam) with FSM effects. |
| 2026-06-12 | Build + then pause the R3 ability engine | Scoped `docs/r3-backlog.md` (decisions: charges in R3, code registry keyed by xws, curated coverage). Built framework: engine-wide timing windows, xws ability registry, equipped-upgrades/pilot-xws threaded into the engine, optional "may" abilities via a `trigger-ability` prompt, effect primitives, minimal charges. Registered an **11-card baseline** (Wedge, Backstabber, Outmaneuver, Juke, Fearless, Predator, Marksmanship + bullseye arc, Fanatical, Trick Shot, Heroic, Crack Shot + upgrade charges) — all behaviour-only paraphrases. Builder shows an "ability" badge + note; charges/Force as roster pips. **Decided to defer the broad card sweep to post-R4** (a survey showed ~80% of cards need R4 mechanics). |
| 2026-06-12 | Rework local play UX | Sandbox is the single **persistent local workspace** living in the **Game tab** (not a sidebar takeover); board persists across enter/exit; a reversible **Turn-based toggle** plays the current board with full rules and snapshots positions back on leave. Dropped Custom hot-seat (sandbox subsumes it) and Quick-match presets. Sandbox is **local-only** (not in online). Dial moved to a collapsible bottom HUD; disclaimer pinned to the sidebar footer. |
| 2026-06-11 | R2-M5/M6: obstacles + sandbox | Asteroids/debris in the engine per **XWA** Rules Reference (pulled from xwing.life): move-through/overlap effects, obstructed = +1 defence die, range-0 = no attack; circle geometry; rolls as events. Pre-game **placement editor** (drag, validity, shuffle) seeding hot-seat + online. Free-play **sandbox** (T-S1). |
| 2026-06-11 | R2-M4 accounts | **OAuth on the Worker** (Google + Discord) chosen; stateless JWT sessions, users + saved squads in D1; guest-id linked on sign-in; builder marks signed-in state. User registered the OAuth apps and set secrets; verified end-to-end (Google confirmed working). |
| 2026-06-11 | Build the R2 squad builder + data layer | **Full xwing-data2 pull** via a generator → committed snapshot (95 ships, all factions, upgrades). Verified points against xwing.life and **overlaid the authoritative XWA 50P 2.0 points** (cap → 50). Multi-faction selection, two-step ship→pilot picker, **per-pilot loadout/upgrade slots**, **XWS import/export**, save/load squads (localStorage + account sync). Fixed the **dial vocabulary** to the full XWA set (Segnor/Tallon/reverse + purple). |
| 2026-06-11 | Stand up the Cloudflare server + deploy | Worker + one Durable Object per game (hibernatable WebSockets) + D1, web-push (VAPID). Deployed; client on Pages. Online lobby: each side brings its own squad; the DO assembles when both join. |
| 2026-06-11 | Finish R1 (M4 client + cleanup) | Hot-seat client: board-first 3-region shell (minimal top nav, side flyout tabs, dark-glass bottom HUD), SVG board with pan/zoom + accurate firing arc, dial UI. Fixed a movement off-by-one (rear-of-base seats at template end). Non-private events visible in online logs. |
| 2026-06-10 | Drive M1–M3, clean per-milestone commits | Built the pure engine in `@xwing/engine`: **M1** core (event-sourced reducer, phase FSM, pending decisions, RNG, view projection), **M2** geometry (templates, SAT collisions, arcs/range), **M3** combat (named timing windows, dice/range, modification, damage, win condition). 56 tests; each milestone a separate commit. Launch-tracker Web-MVP flags updated for engine-complete items. Stops before M4 (fun gate / 3D art) and M5–M6 (Cloudflare account). |
| 2026-06-10 | Begin implementing R1; track progress | Tracking via `docs/r1-progress.md` (ticket source of truth), not the launch tracker (which is a coarse executive view). Completed **T0.1**: pnpm monorepo (engine/data/client/server), TS strict, ESLint 10 + Prettier, Vitest, GitHub Actions CI. All checks green. |
| 2026-06-10 | Set up a `.claude/` config folder to track and maintain requests | Created `.claude/settings.json`, root `CLAUDE.md` (project context + locked decisions), and this log. |
